{
  "mcpServers": {
    "gestalt": {
      "command": "sh",
      "args": [
        "-c",
        "for s in \"${CLAUDE_PLUGIN_ROOT}/scripts/mcp-serve.sh\" \"${GESTALT_LAUNCHER:-}\"; do [ -n \"$s\" ] && [ -x \"$s\" ] && exec \"$s\"; done; exec npx -y @tienne/gestalt serve"
      ]
    }
  }
}
